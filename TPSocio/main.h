#ifndef MAIN_H_INCLUDED
#define MAIN_H_INCLUDED
#define ES_MINISCULA(x) ((x)>= 'a' && (x)<='z' ? (x)-32 : x)
#define ES_MAYUSCULA(x) ((x)>= 'A' && (x)<='Z' ? (x)+32 : x)
#include <stdio.h>
#include "funcionces.h"
#include "primitivas.h"

#endif // MAIN_H_INCLUDED
